# Q1 · 2D Fermi–Hubbard resource estimate

Quantitative replacement for the whiteboard sketch on slide 2 of
`Q1_Fermi_Hubbard-1.pdf`. Self-contained: nothing outside this folder is
imported, written, or executed.

| | |
|---|---|
| `fhcost/` | the cost models (`hubbard` → `nisq` / `ftqc` / `classical` → `curves`) |
| `make_figure.py` | 3-panel paper figure → `figures/fh_resource_estimate.pdf` |
| `make_slide.py` | 1-panel slide version → `figures/*_slide.png` |
| `crossover_table.py` | sensitivity sweep → `crossovers.md` |
| `make_record.py` | the shared result record → `RESULTS.json`, and stamps every consumer |
| `check_parity.py` | proves `explorer.html` computes the same model as `fhcost/` |
| `calibration/` | the exact-diagonalisation programs behind the Trotter constants |
| `check_palette.py` | CVD validation of the figure palette (exit 1 on fail) |
| `explorer.html` | interactive version, published as an Artifact |
| `METHODS.md` | derivations, constants, citations, open parameters |
| `vendor/` | copies of three read-only helpers from elsewhere in `~/Desktop/QC` |

```
python3 -m fhcost.selftest       # ~135 checks, pure arithmetic, <2 s, ~27 MB peak
python3 make_record.py           # RESULTS.json + stamps explorer, README, HEADLINES
python3 make_figure.py && python3 make_slide.py && python3 crossover_table.py
python3 check_palette.py         # CVD validation, exit 1 on fail
python3 check_parity.py          # Python vs JavaScript, in a headless browser
python3 make_record.py --check   # exit 1 if any generated artefact is stale
```

### One model, one record

`fhcost/` is the only implementation. Everything else consumes `RESULTS.json`:
the explorer's constants, the headline prose, and the model fingerprint stamped
on the figure, the slide, `crossovers.md` and the explorer's header. **If two
artefacts show different model ids they came from different models and must not
be compared.**

The explorer is still JavaScript, so it is still a second implementation of the
*functions* — but it is not a second set of assumptions, and it is not trusted on
faith. It recomputes eight configurations' worth of intermediates (steps, signal,
gate counts, shot counts, code distance, workspace, factory choice, footprint,
per-shot time) against the Python answers on every page load and prints the
verdict in its own header. `check_parity.py` reads that verdict back out of a
headless Chrome. A 10⁻⁷ nudge to one line of the port trips 209 of those probes,
so it is not a soft check.

Nothing here runs a simulation: the classical entries are closed-form memory and
flop counts, not state vectors or tensors. If the models are ever validated
against a real small-lattice ED run, note that m = 16 already needs ~2.6 GB per
state vector and m = 18 needs ~38 GB — run that on `lenore_remote` (port 60022),
not on this machine, and put a hard memory guard on it.

## Headline

Generated from `RESULTS.json` by `make_record.py`. Nothing below is hand-written;
`python3 make_record.py --check` fails if it has gone stale.

<!-- BEGIN GENERATED by make_record.py -- do not hand-edit -->
- Model `c39380dc022e`. Every number below is read out of `RESULTS.json`; regenerate with `python3 make_record.py`.
- Classical (ED) frontier: **m = 24-26**, an estimated capacity of specified methods, not an impossibility boundary. The tensor-network arm is unbounded by available data (`OPEN_ITEMS.md` O10).
- Mitigated NISQ saturates: m = 12.0 at n = 10^3 to 17.6 at n = 10^8 -- **5 decades of qubits buy 1.47x in m.**
- At n = 10^6: NISQ+PEC 16, STAR 27, surface FT 26, Pinnacle 38.
- Clearing the ED frontier: Pinnacle 4.50e+05, STAR 8.58e+05, NISQ+PEC not reached below n = 1e+10, surface FT 4.04e+06.
- Continuous m is a capacity proxy. At n = 10^6 the feasible integer lattices are NISQ 3x3, FT 5x5.
- Trotter: under the commutator bound mitigated NISQ does not reach even a 2x2 lattice; under the exact-diagonalisation calibration it reaches 16. Curves are drawn as a band between the two.
- Converged answers: at t_max = sqrt(m)/v the Lieb-Robinson buffer is self-defeating -- the best arm at n = 10^6 reaches m = 38, whose own t_max = 3.08 would need m >= 2155 to CERTIFY a thermodynamic-limit answer. Nothing on the figure closes that loop, classical included. A bound failing is not proof of non-convergence (`OPEN_ITEMS.md` O8).
- Error ledger sums to 1.00 of the tolerance, at per-time two-sided 95%.
<!-- END GENERATED -->
